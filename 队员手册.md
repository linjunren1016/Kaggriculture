# Kaggriculture 参赛队员手册

> Kaggle Featured 竞赛｜奖金 $50,000｜队伍规模 **3–5 人**
> 本文档面向所有队员，目标是**避免误读规则**。带 ⚠️ 的是最容易搞错、且搞错代价很大的点。

---

## 0. 一分钟速览

| 问题 | 答案 |
| :--- | :--- |
| 比赛比什么？ | 两个 AI Agent 在农场经营模拟里一对一，720 回合，**钱多者胜** |
| 排行榜怎么算分？ | **Elo 式技能分，只看赢/输/平，不看赢了多少钱** |
| 被追踪几次提交？ | **全部提交中最新的 2 次**（滚动窗口，不是每天重置） |
| 每天能提几次？ | **5 次** |
| 报名截止 | **2026-09-23** |
| 最终提交截止 | **2026-09-30** |
| 课程要求 | 每周报告 + 抽签上台；期末书面报告 |
| 目标 | **胜率**，不是赛季末资金 |

---

## 1. 课程要求（老师侧）

| 项目 | 要求 |
| :--- | :--- |
| 组队 | **3–5 人一组** |
| 每周 | 整理报告，**抽签上台**汇报 |
| 期末 | **书面报告**缴交 |
| 技术栈 | Python 虚拟环境 + Jupyter Notebook |
| 教材 | `The Kaggle Book`（Bojan）—— 随附 PDF |
| 参考视频 | BruceQD（青岛大学）B 站：`space.bilibili.com/484226629` |
| 字幕下载 | `kedou.life/caption/subtitle/bilibili` |

**注意教材的用法**：`The Kaggle Book` 讲的是传统表格/视觉竞赛（特征工程、交叉验证、GBM）。
本竞赛是 **Simulation / Agent 对抗类**，书中内容只有"评测方法论"和"避免过拟合"两部分直接可用，
**不要指望书里有这个比赛的解法**。这个比赛的解法在工作区那份字幕整理稿里。

---

## 2. 比赛机制

### 2.1 基本框架

- 一局 = **30 天 × 24 小时 = 720 回合**（step 0 → step 719）。
- 两名选手各有 10×10 农场，初始只有 **NW 的 5×5** 解锁，另三块要花钱买（$1k / $2k / $4k）。
- 初始资金 **$3000**。
- **第 720 回合结束时银行里的钱多者获胜。仓库里没卖掉的货不算钱。**
- 平局是可能的。

### 2.2 每回合要做三个决定

| 输出字段 | 内容 |
| :--- | :--- |
| `farmer` | 主农民这一回合干什么（一个动作） |
| `hands` | 每个雇工各干什么（一个动作/人） |
| `market` | 市场订单列表，**最多 10 条，且顺序敏感** |

可选动作：移动（上下左右）、`PLANT` / `WATER` / `HARVEST` / `FERTILIZE` / `DIG`、
`BUILD_COOP` / `BUILD_PASTURE` / `FEED` / `CARE` / `COLLECT_FERTILIZER`、
`PICKUP` / `DROP` / `PLACE`、`PASS`。

市场订单：`BUY_SEED` / `BUY_PRODUCT` / `BUY_ANIMAL` / `SELL` / `HIRE` / `BUY_LAND`。

### 2.3 三条容易忽略的硬规则

1. **作物必须每天浇水，动物必须每天喂食。** 连续两天漏掉 → 作物变杂草 / 动物**永久逃跑**。
   ⚠️ 而且**种植当天就算第一天**，新种的作物当天不浇水当晚就变杂草，**没有宽限期**。
2. **市场是双方共享的。** 你卖得越多价格越低。高级品（草莓/甜瓜/牛奶/羊毛）价格极易崩到 **$1 地板**。
   **卖晚了要吃大亏** —— 对手先卖，你的价格就没了。
3. **雇工成本是斐波那契数列**（1, 1, 2, 3, 5, 8, 13, 21…），**每天重置**，所以每天都要重新雇。
   雇到 10 个时成本可能已经超过收益。

---

## 3. ⚠️ 评分机制（本文档最重要的一节）

### 3.1 只看胜负，不看分差

官方 `Evaluation` 页原文：

> "The actual coin difference in a match does not affect the rating change —
> **only the win, loss, or tie outcome matters.**"

含义：

- 排行榜分数是 **Elo 式技能分**。
- **赢 $1 和赢 $50,000，加分完全一样。**
- 所以优化目标是 **胜率**，不是"这局比对手多赚多少"。

> ⚠️ **这是全队最容易走错的方向。** 字幕整理稿里的讲者反复强调：
> *"很多人会去优化'这局比对手多赚多少'，方向错了。只要能稳定多赢一块钱就够了。"*
>
> 具体影响：如果一个方案 A 平均赢 $20,000、另一个方案 B 平均赢 $2,000，
> 但 B 的**胜率**更高，那么 **B 更好**。不要被 margin 迷惑。

**唯一的例外**：当胜率打平时，**margin 变大仍是正反馈信号**（说明策略在改善，只是还没跨过胜负门槛）。

### 3.2 ⚠️ 只有"最新的 2 次提交"被追踪

官方 `Evaluation` 页：

> "only the **latest 2 submissions** are tracked. The latest 2 submissions are
> also used for final leaderboard evaluation."

FAQ 页措辞更明确：

> "Only your **most recent** N are **active**"

**是"全部提交中最后提交的 2 个"，不是"每天最后提交的 2 个"。**

判定依据：原文用的是 *latest / most recent*（最近的），指的是一个**滚动窗口**。
如果是按天计算，官方会写成 "the latest 2 **each day**"。

#### 滚动窗口实际怎么运作

| 时间 | 提交 | 当前被追踪（active）的 2 个 | 发生了什么 |
| :--- | :--- | :--- | :--- |
| 周一 | A | **A** | 只有 1 个，未满 |
| 周一 | B | **A、B** | 满 2 个 |
| 周二 | C | **B、C** | A 被挤出，**失效** |
| 周二 | D | **C、D** | B 也被挤出 |
| 周三 | E、F | **E、F** | C、D 失效 |
| 周三 | G | **F、G** | E 失效 |
| **截止时** | — | **F、G** | **只有这两个进最终评测** |

**关键推论：**

1. **不存在"提交次数浪费"。** 每天可以提 5 次，试验版本随手提，旧的会自动失效。
   想多刷几次不同方案积累对局数据，是**鼓励的**做法。
2. **但旧成绩会清零。** 你上周的高分不会被保留，别指望吃老本。
3. ⚠️ **截止前必须有意安排最后两次提交。** 这是个真实的坑：
   如果你在截止前手忙脚乱地提交了几个**试验版本**，那么最终被评测的
   2 个里可能包含一个**半成品**，它会直接拖累最终成绩。
   **正确做法：确认好两个最佳版本，再按顺序提交。**

### 3.3 上传时的校验

> "When you upload a submission, a **Validation Episode** is run where your agent
> plays against a copy of itself to ensure it runs without errors. If the episode
> fails, the submission is marked as `Error`."

- 这是**自己打自己**，用来确认代码能跑。
- 失败 → 标记 `Error`，可以下载 agent 日志调试。
- ⚠️ **Agent 抛异常会导致整局作废并记 0 分**，所以代码必须有异常兜底。

### 3.4 最终评测

> "At the submission deadline, additional submissions will be locked. Games will
> continue to run for approximately two weeks… A final **Bradley-Terry
> tournament** will be run on those episodes to produce the final leaderboard."

- 截止后所有提交锁定，对局**再跑约两周**。
- 然后跑 Bradley-Terry 锦标赛出最终榜。
- 所以最终排名取决于**截止时在册的那 2 个方案**，以及之后两周的运气。

---

## 4. ⚠️ 榜单会骗人（别被吓住，也别被迷惑）

这三条都出自字幕整理稿的实操观察：

### 4.1 匹配频率会衰减

刚提交时平台会**非常频繁**地安排对局；**2–4 小时后明显变少**；**排名越高，对局越少**。

### 4.2 "榜单假象"

因为高排名选手被安排的对局少、**掉分也慢**，所以：

- 榜上靠前的可能是**十几个小时前的旧方案**；
- 点开回放会发现它**输的局很多**，只是掉分慢。

> ⚠️ **不要因为"我的新方案要对上第 10 名，肯定打不过"就不敢提交。**
> 那些旧方案未必真的强。

### 4.3 同一方案提交两次，分数可能差 1000 分以上

原因：如果某个提交**在早期就撞上一个很强的对手并输掉**，它的分数可能**一直爬不起来**。

> ⚠️ **所以：单次榜单变化不能证明你的改动有效。**
> 榜涨了可能是运气，榜跌了可能也是运气。
> 判断改动是否有效**只能靠线下反事实验证**（见第 6 节）。

---

## 5. 提交操作

```powershell
# 打包（main.py 必须在压缩包根目录）
tar -czf submission.tar.gz main.py

# 提交
kaggle competitions submit kaggriculture -f submission.tar.gz -m "说明文字"

# 查看自己的提交状态（记下 ref / submission id）
kaggle competitions submissions kaggriculture

# 查看某次提交打过的对局
kaggle competitions episodes <SUBMISSION_ID>

# 下载某局回放 / 日志
kaggle competitions replay <EPISODE_ID>
kaggle competitions logs <EPISODE_ID> 0
```

**为什么 submission id 重要**：它是定位问题的钥匙。
例如"某次提交打了 100 局只输 3 局，但这 3 局都输给同一个队"——
必须先用 id 把对应回放拉下来，才能分析"被谁、怎么打赢的"。

**提交契约**：压缩包**根目录**必须有 `main.py`，其中暴露 `agent(obs)` 函数。

---

## 6. 验证方法论（改动是否真的有效）

**核心原则：榜单不能用来判断改动好坏，必须线下验证。**

### 6.1 反事实验证（Counterfactual）

1. 下载**排名靠前选手**的 replay，以及**自己提交的** replay。
2. 把方案里的**某个组件替换掉**，对手的轨迹**保持不变**，重跑。
3. 看两个方向：
   - **挑战集**：原本输的局，有没有翻盘变成赢？
   - **回归集**：原本赢的局，有没有变成输？（⚠️ 这一步最容易被忽略）
4. ⚠️ **必须跑 10–20 个随机 seed。** 单局结论毫无意义 —— 地图（由 seed 决定）本身也是变量。

**真实反例**：整理稿里讲者试过一个改动，在某一局把败局翻转成胜局，
但应用到全部 replay 后发现**新增了 2 局原本赢、现在输的对局**，
总失败局数从 1 变成 3 —— **反而更差了**。

### 6.2 线下 Agent 互相对抗

- 把各版本 agent 与 Code 区开源 agent **互相打**，记录全部轨迹。
- 优势：**没有任何限制**，不用消耗线上提交次数。
- 比只读线上 replay 更全面。

### 6.3 评测该看哪些指标

不能只看平均胜率。要看：

- 胜率
- **胜率矩阵**（谁克制谁）
- 平均 margin（分差）
- 方差 / **最差情况**
- 成对对局的 delta

### 6.4 提交前必做

1. **重新拉一次**最新榜单的 replay（你折腾的几个小时里，别人可能出了新方案）。
2. 加上自己 submission 新增的 replay。
3. 再对抗、再评测一遍，确认没问题再提交。

---

## 7. 已确认有效的具体改动

来自字幕整理稿，讲者靠这些进入 public 榜金牌区（第 8–9 名，2817.6 分）：

| 改动 | 具体做法 |
| :--- | :--- |
| **遇杂草先挖地** | 单位脚下是杂草挡住生产动作时，先 `DIG` 再继续原计划 |
| **提前卖货** | 售卖**按价值排序**，并且**从 step 716 就开始清仓**，而不是等到 step 719 |
| **临近结束停止买种子** | 很多选手最后一步还在买种子，等于白花钱 |

**排查前先看这条**：整理稿指出"很多选手在最后一步还有东西没卖出去" —— 这是低垂的果实。

---

## 8. 开发路线

### 8.1 标准流程

1. 理解规则（相对简单）
2. **跑通 baseline** —— 官方 baseline 或 Code 区开源 baseline
3. **下载 replay 做研究**，拆解公开方案
4. **一次只改一件事**
5. **反事实验证 + agent 对抗**
6. 用全面的评测结果决定是否采纳
7. 持续迭代

### 8.2 ⚠️ 先做规则，不要急着建模

整理稿最重要的经验：

> "很多人做 Kaggle 第一反应就是建模型，但在这个比赛里是另一条路：
> 先下载数据、做分析、制定规则、用规则去修正方案。"

**什么时候才值得建模？**
当规则多到难以用简单流程串起来，**并且** replay 积累到几千、几万条量级时。
一开始基于几百条 replay 分析完，你想做的操作**往往只是一个公式或一个系数**。

关于强化学习：整理稿里讲者的建议是**别直接上**。
棋盘 100 格、角色多、动作多、还有市场供需，复杂度极高。
建议路径是**从后往前、从局部开始**（先做 step 716，再往前推）。

### 8.3 ⚠️ 从后往前优化

**不要从前面改。** 从 **step 716 往前**做分叉：

- 后续动作少，对终局的影响**能直接看出来**；
- 贪心 / 启发式 / 穷举才跑得动。

关键节点候选：**step 80 / 160 / 340 / 716**。

### 8.4 线上 vs 线下的分工

| | 线下 | 线上 |
| :--- | :--- | :--- |
| 复杂度 | 可以用复杂模型、大算力、MCTS | 文件 < 100MB，内存/CPU 受限 |
| 但 | 只用于**研究** | 提交时必须**简化、规则化** |

> "一个复杂但有效的模型，往往可以用一套简单规则做近似等价的替代，然后再提交上线。"

---

## 9. 工作区资产（已有）

路径：`D:\hiwsk\Documents\deepseek工作区\kaggriculture\`

| 文件 | 说明 |
| :--- | :--- |
| `main.py` | 可直接提交的 Agent（通过 Kaggle 式自对局校验） |
| `submission.tar.gz` | 打包好的提交包 |
| `README_参赛说明.md` | 完整技术说明（含踩坑记录） |
| `validate_submission.py` | **提交前必跑**，复现 Kaggle 的 Validation Episode |
| `benchmark.py` | 本地评测（对 `pass` / `random` / `starter`） |
| `tune.py` | 参数扫描 |
| `probe_crop.py` | 单株作物生命周期探针 |
| `README.md` / `AGENTS.md` | 官方规则全文与上手指南 |
| `_docx/doc.txt` | **字幕整理稿全文纯文本（1133 行，建议全文检索一遍）** |

### 环境注意

⚠️ **默认的 Python 3.14 用不了** —— `kaggle-environments` 依赖 `pygame`，
而 `pygame` 没有 3.14 的预编译 wheel，源码编译会失败。**必须用 Python 3.12。**

```powershell
uv venv --python 3.12 .venv
uv pip install --python .venv\Scripts\python.exe -U kaggle-environments
```

---

## 10. ⚠️ 踩坑速查（每一条都是实际踩过的）

| 坑 | 后果 | 正确做法 |
| :--- | :--- | :--- |
| `HARVEST` 在 `first_yield_day` 之前是**静默空操作** | 单位对着同一块地无限收割，**完全不动** | 按作物自己的 `first_yield_day` / `max_yield_day` 判断，别只看 `yield_units > 0` |
| 同一回合多个单位请求同一种**稀缺种子** | 引擎**作废该作物全部请求**，一个都种不下去 | 做**种植预约**，按单位顺序预留种子 |
| Kaggle 文件加载器取**模块全局最后一个可调用对象** | 文件里 `agent` 之后的任何模块级 lambda 都会被当成 Agent | `agent` 必须定义在文件**最末尾** |
| 用 PowerShell `Set-Content -Encoding UTF8` 写 Python | 写入 **BOM**，`compile()` 抛 `SyntaxError` | 用编辑器保存为**无 BOM** UTF-8 |
| Agent 抛异常 | **整局作废记 0 分** | 加异常兜底装饰器，出错返回 `PASS` |
| `PASS` 写成裸字符串 `"PASS"` | 引擎读 `op[0]` 拿到 `"P"` | 一律写成 `["PASS"]` |
| 本机默认 Python 3.14 | 装不上环境 | 用 Python 3.12 |

---

## 11. 待办清单

### 全队（尽快）
- [ ] 确认 **3–5 人**队伍，明确分工（数据/实现/验证/报告）
- [ ] ⚠️ 注意 Kaggle 的 **Team Merger Deadline**，合并队伍也有截止日
- [ ] 在 Kaggle 页面核对 **Entry Deadline** 与 **Final Submission** 的确切日期
- [ ] 浏览器里**手动玩一局**，熟悉动作与市场机制（比读文档快）
- [ ] 通读 `_docx/doc.txt`（字幕整理稿）

### 技术（尽快）
- [ ] 提交一版 baseline，**拿到 submission id**（这是后续一切分析的前提）
- [ ] 跑通 Jupyter Notebook 接上 Python 3.12 虚拟环境
- [ ] 下载 Code 区点赞最高的开源方案（整理稿点名 **Kaito V27**）当实验对象
- [ ] 写批量下载 replay 的脚本（top 10 队伍 × 每人 20 条，上限 200 条，按 replay id 去重）
- [ ] ⚠️ 剔除"分高但提交久远、且正在连败"的队伍，它们的 replay 不值得下

### 验证（采纳任何改动前）
- [ ] 搭好反事实验证流程
- [ ] 跑 **10–20 个 seed**，同时看挑战集和回归集
- [ ] 建线下 agent 对抗环境

### ⚠️ 截止前
- [ ] **有意安排最后两次提交**，确保在册的 2 个都是最佳版本
- [ ] 别让试验版本成为最后提交

---

## 12. 常见混淆点对照表

| 容易混淆 | 正确理解 |
| :--- | :--- |
| "最近 2 次" = 每天最近 2 次？ | ❌ **是全部提交中最近 2 次**（滚动窗口） |
| 分数高 = 方案强？ | ❌ 可能是提交早、掉分慢，点开回放可能输很多 |
| margin 大 = 更好？ | ❌ 只看胜负；margin 仅在胜率打平时作为正反馈信号 |
| 榜涨了 = 我的改动有效？ | ❌ 可能是运气；必须做反事实验证 |
| 提交次数要省着用？ | ❌ 每天 5 次，旧的会自动失效，**鼓励多提** |
| 但要小心？ | ✅ 截止时**在册的 2 个**决定最终成绩 |
| 先建模型？ | ❌ **先做规则**；几千条 replay 之后再考虑建模 |
| 从第 0 步开始优化？ | ❌ **从 step 716 往前** |
| 书里有解法？ | ❌ `The Kaggle Book` 讲传统竞赛；本比赛是 Agent 对抗 |
