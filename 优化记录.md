# main.py 优化记录

用 `sparring_melon.py`（第二策略陪练）+ `duel.py`（成对对抗）+ `holdout.py` / `evaluate.py`
（留出集验证）迭代优化 `main.py`。

**验证纪律**：每次改动都要过两关 —— **挑战集**（对 `pass` / `starter` / 陪练）
和**留出集**（未参与调参的新种子）。种子分三档：

| 种子 | 用途 | 是否可用于决策 |
| :--- | :--- | :--- |
| 4000–4004、8000–8009 | 开发调参 | 可以，但结果仅供筛选 |
| 11000–14011 | 配比实验的复验批 | 用过即"变脏"，换新批 |
| **9000–9019** | **密封验收**（`evaluate.py --mode accept`） | **只在验收时看，禁止用于筛选** |

所有改动都在 git 里，可精确对比与回退。

## 结果总览

| 指标 | 优化开始 | 现在 | 变化 |
| :--- | ---: | ---: | ---: |
| **密封验收平均资金** | $26,007 | **$31,192** | **+19.9%** |
| 对 `pass` margin（验收） | $23,587 | **$28,614** | +21.3% |
| 对 `starter` margin（验收） | $21,987 | **$27,338** | +24.3% |
| 对陪练 margin | $4,118 | **$20,383** | **+395%** |
| 对 `pass` 绝对资金 | $18,814 | **$32,000+** | +70% |
| 地块利用率 | 49.8% | 55.2% | +5.4pt |
| 移动动作占比（农民） | 43.2% | 39.2% | −4.0pt |

> 注："优化开始"= `bbffa32`（即线上在册的 405.3 分版本）；
> 密封验收的 $26,007 是**已含前两项改动**的中间态基线。

## 采纳的改动

### 0. 作物配比向甜瓜倾斜（`0ba4b7e` 之后）

**问题**：原配比里小麦占 0.34，理由是"小麦兼作牲畜饲料"。但
`MAX_LIVESTOCK = 0` 意味着**没有任何东西消耗这份饲料** —— 这个权重纯属机会成本。

**改法**：`wheat .18 / melon .40 / carrot .14 / tomato .20 / strawberry .08`。
依据是甜瓜的单位地块价值最高（42.5 元/地块/天 vs 小麦 21.2），
且番茄的稀缺曲线是 `hinge`（`below_target` 0.40），价格在常规需求下贴近基础价。

**验证（4 批独立 12 种子，全部全新）**：

| 批次 | 候选A(m.40/t.20) vs 原配比 | 胜出 |
| :--- | ---: | ---: |
| 11000–11011 | — | 见下批 |
| 12000–12011 | +28.0% | 12/12 |
| 13000–13011 | +33.1% | 12/12 |
| 14000–14011 | +13.6% | 11/12 |

**最优点很尖**：甜瓜 0.38（−16%）和 0.42（−12%）都明显更差，说明 0.40 是真实峰值，
不是平坦的高地。

**验收结果**：$26,007 → **$31,192（+19.9%）**。

### 1. 任务按最近距离分配（`092fa44`）

**问题**：任务按列表顺序发放（`tasks[:1 + len(hands)]`），完全不管单位站在哪，
于是单位经常**穿过一块更近的地，去干另一块远处的地**。

**证据**：农民 43.2%、雇工 59.1% 的动作是移动 —— 劳动力预算被走路吃掉了，
地块利用率因此卡在 49.8%。

**改法**：贪心最近分配 —— 按单位顺序，每个单位取剩余任务里离自己最近的那个，
取走即从池中移除（`_assign_tasks`）。

**效果**（4 种子 × 2 座位）：
- 对 `pass` 绝对资金 $18,814 → $24,070（**+28%**）
- 对陪练 margin $4,118 → $7,561（**+83%**）
- 对 `starter` margin $20,911
- 三个对手**全部改善，无回归**

### 2. `HIRE_CAP` 6 → 8（`0ba4b7e`）

**改法**：雇工上限从 6 提到 8。

**验证过程（这次的方法论值得记住）**：
1. 在种子 4000–4002 上扫 4/6/8/10/12 → 8 最好（$26,182），6 次之（$24,158）
2. 在**全新**种子 5000–5004（5 个）复验 → 8 只赢 3/5，平均 +5.8% → **判为不确定，暂不采纳**
3. 扩到**全新**种子 6000–6011（12 个）复验 → 8 赢 **10/12**，平均 **+13.8%** → **采纳**
4. 继续验证 8 vs 10（同 12 个种子）→ 10 只赢 **1/12**，平均 **−29.6%** → 确认 8 是拐点

**教训**：5 个种子得出的"过拟合"结论其实是**样本量不足**。斐波那契成本曲线的拐点
就在 8 附近，10 开始明显亏。

**效果**：对 `pass` $24,070 → $25,731；对陪练 $7,561 → $10,538。

## 测了但**否决**的改动

### 饲料小麦采购加守卫

`_market_orders` 里原本无条件维持 `live_animals + 6` 的小麦储备，而
`MAX_LIVESTOCK = 0` 意味着永远不会有牲畜消耗它 —— 看起来是纯烧钱。

**实测：改前 $18,814 vs 改后 $18,788，差 0.14%，是噪声。**
虽然 `BUY_PRODUCT` 被调用 1722 次，但前面的资金余量条件大多把订单挡住了。

**结论**：因果推断错了。改动保留（逻辑上正确），但**不宣称它带来收益**。

> 这条比采纳的改动更值得记住：**看着像 bug、推理也通顺的改动，可能完全没有效果。**
> 只有实测能分辨。

### 加大出售批量 `SELL_BATCH`（种子 4000–4004）

诊断发现季末市场价普遍**高于**基础价（草莓 $313 vs 基础 $120、番茄 $153 vs $60），
像是"卖得太保守、需求没开发"。扫 6/12/20/40：

| SELL_BATCH | 平均资金 |
| ---: | ---: |
| **6** | **$25,754** |
| 12 | $24,871 |
| 20 | $22,648 |
| 40 | $20,833 |

**加大批量反而更差** —— 卖得快会把价格砸下去。当前保守出售是对的。
季末高价不是浪费，而是"尚未充分开发需求"的均衡态。

### `HIRE_CAP` 7 与 9（种子 10000–10009）

8 是拐点，但 7 和 9 从未测过，补测：

| 对比 | 差异 | 胜出 | 结论 |
| :--- | ---: | ---: | :--- |
| 8 vs 7 | 8 领先 +4.3% | 8 赢 5/10 | 无差异 |
| 8 vs 9 | 8 领先 +10.5% | 8 赢 7/10 | 无差异/8 略优 |

**8 保持不变。**

## 下一步候选（尚未验证）

1. **移动开销仍占 39–50%**：最近邻分配已把农民移动从 43.2% 降到 39.2%，
   但雇工仍约 50%。可考虑按单位划分固定作业区（每人负责一片），
   减少跨区走动。
2. **利用 `town.unlocked_shops`**：商店每 3 天随机解锁，决定了哪类商品有需求。
   当前作物配比是固定的，没有利用这个公开信号。但作物生长周期 4–10 天，
   调整有滞后，收益不确定。
3. **`WATER` 占 33–38%**：劳动力接近饱和，继续扩张可能得不偿失。

## 尚未验证的关键假设

**以上全部是本地指标。** 密封验收本地资金 +19.9% **不保证**线上技能分同步提升 ——
线上对手是真人方案，本地对手只有内置基线和自建陪练。

线上当前 **405.3 分**（在册两个提交均为本次优化**之前**的版本）。
必须提交后观察线上分数才能验证本地指标与线上是否对齐。

## 工具说明

| 脚本 | 用途 |
| :--- | :--- |
| `evaluate.py` | **固定评测台**：`--mode tune`（开发）/ `--mode accept`（密封种子，禁止用于筛选） |
| `duel.py` | 两个 Agent 成对对抗（双座位），`--b pass` 可测绝对资金 |
| `holdout.py` | 留出集验证：用未参与调参的种子复验改动 |
| `mix_experiment.py` | 作物配比实验（参数化 `TARGET_MIX`，多候选对比） |
| `tune2.py` | 参数扫描（自动重载 main 模块，避免状态残留） |
| `diag_main.py` | 行为诊断：动作分布、地块利用率、资金曲线 |
| `sparring_melon.py` | 第二策略陪练（价格感知的边际收益出售） |
| `validate_submission.py` | 提交前自对局校验（复现 Kaggle 的 Validation Episode） |

