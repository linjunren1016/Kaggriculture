# 第四轮：扩大挖掘 + 一次重要的评测纠错

> 承接 `第三轮提升.md`（采用 V38）。本轮扩大到 14 个公开 notebook，
> 提取更多 agent，并**发现并修正了我自己评测方法的一个漏洞**。

## 一、本轮新增提取

| Agent | 大小 | 来源 notebook | 说明 |
| :--- | ---: | :--- | :--- |
| **V14** | 26,304 B | `84-84-base-public-holdout-v14-clone-preemption` | 重建 Kakuteki/mrgrishninsb 路线 + 近克隆抢占 |

之前已提取：C92 / C94 / C95 / KaitoV27 / V38。

累计扫描 **14 个公开 notebook**，得到 **6 个可运行 agent**。

> 有 3 个 notebook 返回 403（`v16-rc2`、`shop-router-reactive-v5`、`177-180-top-54`），
> 已删除或私有，无法获取。

## 二、⚠️ 重要纠错：我的循环赛记账有 bug

第一版 `roundrobin.py` 产出的胜负矩阵**不对称**：

```
C94 对 V38 记 10W   ←→   V38 对 C94 只记 2W
```

同一批对局不可能这样。**矩阵不对称 = 记账错误**，那份排名不可信。

### 修正后的判定方法

重写为 `roundrobin2.py`：
- 每一对 (i, j) 明确跑两个座位，用「谁的座位」判断胜负；
- **最后断言矩阵对称**，不通过就报错；
- 另加了最小实现 `h2h.py`：**逐局打印双方资金与胜负**，不聚合。

## 三、🔍 最重要的发现：胜负是种子依赖的

对同一个问题（V38 vs C94），不同种子集给出**完全相反**的结论：

| 评测 | 种子 | 结果 |
| :--- | :--- | :--- |
| 专项对打（40 局） | 400000–400019 | **V38 29-11（72.5%）** |
| 修正版循环赛 | 600000–600005 | **C94 8-4（67%）** |
| 最小实现复核 400000 批 | 400000–400019 | V38 29-11（**一致**）|
| 最小实现复核 600000 批 | 600000–600005 | C94 8-4（**一致**）|
| **决定性判定（80 局）** | **700000–700039** | **V38 51-29（63.7%）** |

**两个评测都没有错 —— 是种子集的差异。** 我此前把 400000 那批的 72.5%
当成"V38 更强"的证据，那其实是**方差**，不是优势。

### 由此得到的方法论教训

> **在单一小种子集上比较两个 agent，结论可能是纯粹的种子运气。**
> 我这次的 400000 批（40 局）给出 72.5%，而换一批种子立刻反转成 33%。
> 这与 `rank-your-agent` 作者的警告完全一致：
> *"调参用一套种子，决策用另一套。"*

**正确做法**：判定两个 agent 的高下，至少需要**几十局、且种子集与调参时不同**。
本轮最终用 80 局（700000–700039）才得到可信的 63.7%。

## 四、候选淘汰情况

| Agent | 对 V38 / 结论 | 判定 |
| :--- | :--- | :--- |
| **V38** | —（当前提交版）| **保留** |
| C94 | 80 局：**V38 51-29（63.7%）** | 淘汰（V38 更优）|
| C95 | 12 局：**V38 8-4** | 淘汰 |
| KaitoV27 | 12 局：**V38 10-2** | 淘汰 |
| V14 | 对 C94 **0-12** | 淘汰（明显弱）|

### 关于 V14 的诚实说明

V14 的机制**看起来很强**（重建顶分选手路线 + 近克隆抢占 + 数量守恒的时机挪用），
但实测**明显弱**：对 C94 **0 胜 12 负**。

它的自述也给出了解释：*"updated Kakuteki / mrgrishninsb route"* ——
它重建的是**某一时点**的前排路线，而前沿已经继续演化。**固定路线会过时。**

它能对 `starter` 打出 $138,547（三者中居中），但在真正对抗中赢不了对局 ——
**又一次印证"资金不是判据"**。

## 五、V38 的最终验证数据

| 项目 | 结果 |
| :--- | :--- |
| vs C94（80 局，种子 700000–700039） | **51W-29L（63.7%）** |
| vs C95（12 局） | 8W-4L |
| vs KaitoV27（12 局） | 10W-2L |
| 对参考天梯（10 对手，60 局） | **60W-0L（100%）** |
| 自对局校验 | 3 种子全 `DONE` |
| SHA-256 | `c26402b67a0d04a46348353069645b1a49c3cb3df6df69d7fa35d8adbbdbeae4` |

## 六、当前状态与下一步

**`main.py` 保持 V38**（已提交，线上反馈良好）。

### 下一步方向（按可行性排序）

1. **继续扩大 agent 池**。本轮又有 3 个 notebook 因 403 拿不到；
   可以换搜索策略（按作者、按标签）找更多可提取方案。
   已验证的两条提取路径：
   - 压缩封装：`_AGENT_B64_PARTS` / `_AGENT_B85_PARTS` → base64/base85 + zlib
   - 明文封装：`TOP_AGENT_FILES = {'main.py': '<源码>'}`

2. **自研改动：动态路线评估**。V38 内嵌多条路线但切换逻辑是固定的。
   若能**在运行时评估当前路线是否适合这局**（例如观察对手是否也在用同一条），
   理论上可再进一步 —— 但必须在**留出种子**上验证，不能只看一批。

3. **市场时机层**（V14 的方向本身是对的）。V14 的"近克隆抢占 + 数量守恒"
   是唯一不靠增产的改法。它整体过时，但这个**机制**可以从 V38 上重新实现。
   不过按本轮教训，任何此类改动都需要**几十局、跨多种子集**验证。

4. **不要做的事**（已多方验证为负）：
   - 买 SE 第四象限扩张
   - 单纯追求资金产出（本轮 V14 再次印证）
   - 在单一小种子集上判定 agent 优劣（**本轮最大教训**）

## 七、工具清单（本轮新增）

| 脚本 | 用途 |
| :--- | :--- |
| `roundrobin2.py` | **修正版循环赛**，带对称性断言（取代有 bug 的 `roundrobin.py`）|
| `h2h.py` | **最小实现对打**，逐局打印，不聚合 —— 用于复核其他评测结论 |
| `diag_v14.py` | 逐个 agent 跑通性诊断（对 starter 一局，暴露原始异常）|
| `candidates/` | 候选 agent 集合（V38 / V14 / C94 / C95 / V27）|
| `dump_md.py` | 提取 notebook 的 markdown，便于学习其结论 |
