# 第八轮：交易频率对齐实验（两条均否决）

> 承接 `第七轮提升.md`。上轮发现"9 月 meta 的 SELL/ BUY_SEED 频率与 V38 差异明显"，
> 本轮把这条线索做成两个可测实验。**两条都被数据否决。**

## 一、先摸清 V38 的市场结构

`agent()` 每回合的流水线（984–998 行）：

```
action = 表内动作                     ← 预录的 720 步动作表（base85 编码）
action = _weed_repair_action(...)     ← 杂草修复
action = _v17_feed_guard(...)         ← 饲料守卫
action = _v17_room_evac(...)          ← 仓库腾挪
action = _repay_shift(...)            ← 偿还上轮挪用的卖单
action = _rank_sell_slots(...)        ← 按价格冲击重排卖单
action = _preempt_shift(...)          ← 提前卖出（抢占）
action = _v17_r5_counter(...)         ← 反制 R5 家族
action = _v17_md_counter(...)         ← 反制 MD 家族
action = _v17_room_guard(...)         ← 仓库守卫
action = _terminal_liquidation(...)   ← 终局清仓
action = _align_hands(...)            ← 雇工对齐
```

可调常量：

```
_PREEMPT_ENABLED = True
_PREEMPT_FRACTION = 1.0
_PREEMPT_MAX_BATCH = 12
_PREEMPT_MAX_CLONE_DISTANCE = 6
_PREEMPT_MIN_PRICE_RATIO = 0.0
_PREEMPT_MIN_FUTURE_QUANTITY = 4
_PREEMPT_START = 120
_PREEMPT_STOP = 680
```

## 二、实验一：调 `_preempt_shift`（提前卖出）—— 无效

假设：V38 每局 SELL 505 次（比 9 月 meta 的 401 多 26%），
多出来的来自 `_preempt_shift`，调小它即可对齐。

扫 6 种配置（种子 990000–990003，各 4 局）：

| 配置 | SELL/局 | BUY_SEED/局 | 平均资金 |
| :--- | ---: | ---: | ---: |
| baseline | 484.0 | 101.0 | 147,449 |
| no_preempt | 484.0 | 101.0 | 147,449 |
| batch6 | 484.0 | 101.0 | 147,449 |
| batch2 | 484.0 | 101.0 | 147,449 |
| minq12 | 484.0 | 101.0 | 147,449 |
| short | 484.0 | 101.0 | 147,449 |

**六种配置结果完全一致** —— 说明 `_preempt_shift` 在此场景**根本没触发**。
原因写在代码里（第 308 行）：它要求 `_clone_distance(obs) <= 6`，
即**对手正在抄同一条路线**才激活；对 `pass` 对手不满足。

**结论**：V38 的 484 次 SELL **全部来自预录动作表**，不是覆盖层。
要改只能改 base85 编码的表 —— 那会破坏哈希完整性，且是混淆数据，不可行。

### 附带的重要澄清

9 月 12 个 agent 的 SELL 分布是 **237–504（均值 401，方差很大）**，
V38 的 484 **落在其范围内** —— 所以"SELL 偏多"本身不是异常。

## 三、实验二：BUY_SEED 补足 —— 否决

假设：9 月 meta 每局 BUY_SEED 177 次，V38 只有 ~101（低 43%），补上应有益。

做法：在 V38 之上加一层「种子补足」覆盖 ——
按目标配比检查「已种 + 库存」，不足则追加 `BUY_SEED`，受 10 条订单上限约束。

结果（种子 991000–991004，各 5 局）：

| 配置 | SELL/局 | BUY_SEED/局 | 平均资金 | 最低 |
| :--- | ---: | ---: | ---: | ---: |
| V38 基线 | 491.0 | 99.4 | **173,416** | **164,089** |
| 加种子补足 | 491.4 | 112.4 | 159,552 | 145,901 |

**平均 −$13,864，占优 0/5。**

而且 BUY_SEED 只从 99.4 升到 112.4（离 177 还很远）就已经有害 ——
说明**这个方向本身就是错的**，不是实现不够力。

## 四、本轮的方法论收获

1. **修饰性覆盖层难以改进一个良好调优的预录 artifact。**
   V38 的表内动作经过大量离线演化，外层加规则几乎必然损伤它。
2. **"频率对齐"是错的思路。** 9 月 meta 的 BUY_SEED 高，
   是因为它们**在跑更大的农场 / 不同配比**，不是因为"多买种子"本身好。
   把表象统计对齐，不等于对齐了机制。
3. **行为统计是相关而非因果。** 高 rating 组的各种次数多，
   可能只是"规模更大"的结果，照搬计数不会带来同等收益。
4. **验证仍然救命**：两个看似有理有据的假设（一个有官方统计支撑）
   实测都是负收益。没有实测就会采纳它们并提交。

## 五、累计的被否决改动（本 session）

| 改动 | 结果 |
| :--- | :--- |
| 饲料小麦采购加守卫 | 噪声（±0.14%）|
| 加大出售批量 SELL_BATCH | 更差（6 最优）|
| HIRE_CAP 7 / 9 | 无差异（8 最优）|
| 限制地块预算（仿"先铺满"）| 资金噪声，尾部风险更差 |
| 加 FERTILIZE（空档期施肥）| −$2,909，0/5 |
| 调 `_preempt_shift` | 完全不触发，无效果 |
| **BUY_SEED 补足** | **−$13,864，0/5** |

## 六、现在的判断

**V38 内部难改**（预录表 + 已调优的覆盖层），
**外部对齐也无效**（两条实验均否决）。

而官方数据显示：V38 的农场层、市场层、产出都已与高 rating 档对齐，
**剩下的差距主要是评分累积**（初始 600，爬到 2,400–2,700 需 700–800 局）。

### 建议

1. **保持 V38 不变，让它持续积累对局。** 不要再做修饰性改动。
2. **定期记录分数走势**，验证"爬升-回落"模式。
3. **若必须再动方案**，唯一有意义的方向是**换一个更强的基座**
   （而不是修补 V38），判据：对 V38 胜率 > 50%（多子集、几十局）。

## 七、产物

| 文件 | 说明 |
| :--- | :--- |
| `preempt_exp.py` | `_preempt_shift` 常量扫描（证明其不触发）|
| `seed_exp.py` | BUY_SEED 补足实验（已否决）|
| `_docx/preempt_exp.txt` / `_docx/seed_exp.txt` | 原始结果 |
